module m
provider P { kind external enabled false dry_run_only true requires feature_flag requires approval }
adapter A {
  provider P
  mode experimental
  execution disabled
  network allowed
  secrets denied
  filesystem none
}
