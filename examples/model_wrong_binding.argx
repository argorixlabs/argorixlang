module Invalid
capability model.invoke { level safe }
type Input { value: string } type Output { value: string }
model Guard { provider simulated capability model.invoke input Input output Output }
agent A { receives Input capabilities { model.invoke } models { Guard } on Input as input { ask Guard with other } }

