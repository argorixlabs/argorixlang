core 0.1;
module refusals.struct_scrutinee;

struct Point { x: u64, y: u64, }

pub fn argorix_main() -> u64 {
    let point: Point = Point { x: 1u64, y: 2u64 };
    match point {
        other => other.x,
    }
}
