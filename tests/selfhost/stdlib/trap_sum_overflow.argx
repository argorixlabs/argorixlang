core 0.1;
module selfhost_stdlib.trap_sum_overflow;

// stdlib.vector.sum does not wrap: a total past u64::MAX traps.

import stdlib.vector;

pub fn argorix_main() -> u64 {
    let values: Array<u64, 2> = [18446744073709551615u64, 1u64];
    vector.sum(values.as_slice())
}
