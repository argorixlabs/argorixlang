core 0.1;
module demo.main;

import demo.helper;

pub fn argorix_main() -> u32 {
    let first: u32 = helper.nothing(1u32);
    // A type is not a value, and a private one is not either.
    let second: u32 = helper.Pair;
    first + second + helper.MISSING
}
