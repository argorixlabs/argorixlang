core 0.1;
module runtime.utf8_trap;

pub fn argorix_main() -> u64 {
    let fixed: Array<u8, 1> = [255u8];
    let raw: bytes = fixed.as_bytes();
    let text: string = raw.decode_utf8_or_trap();
    text.length()
}
