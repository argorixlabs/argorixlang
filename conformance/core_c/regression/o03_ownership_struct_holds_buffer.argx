core 0.1;
module regression.o03_ownership_struct_holds_buffer;

struct Holder { values: Buffer<u8>, count: u64, }

pub fn argorix_main() -> u64 {
    let mut b: Buffer<u8> = Buffer::new();
    b.push(1u8);
    let h: Holder = Holder { values: b, count: 1u64 };
    h.count
}
