core 0.1;
module regression.m01_imports;

import regression.m01_helper as helper;
import regression.m01_helper as h;

fn bump(value: u32) -> u32 {
    value + 100u32
}

pub fn argorix_main() -> u32 {
    let twice: u32 = 1u32;
    helper.twice(h.SCALE) + twice + bump(0u32) - 99u32
}
