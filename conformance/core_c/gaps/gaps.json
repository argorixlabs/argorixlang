{
  "schema_version": 1,
  "recorded_commit": "9c77061",
  "recorded_with": "gcc 12.2.0, -std=c11 -Wall -Wextra -Werror",
  "purpose": "Known defects of the transitional C backend, each with the behaviour it reproduces today, so a fix is noticed and can be promoted. The sixteen defects of issue #27 were fixed in ESP-009.B and now live as regression cases in ../regression/cases.json; the three recorded here were found by the ESP-009.C differential work and reported as issue #39. Each program is valid Core 0.1 that argorixc core-check accepts.",
  "gaps": [
    {
      "id": "g17_loop_statement",
      "file": "g17_loop_statement.argx",
      "kind": "emit_rejected",
      "spec_expected_stdout": "ARGORIX_RESULT:6",
      "signature": "value block needs an expected scalar type",
      "note": "`loop { .. break; }` used as a statement. spec/core/evaluation.md says `while` and `loop` are statements unless every `break value` of a `loop` agrees, so the statement form needs no value at all. The emitter asks the loop's block for a scalar type anyway."
    },
    {
      "id": "g18_loop_break_value",
      "file": "g18_loop_break_value.argx",
      "kind": "emit_rejected",
      "spec_expected_stdout": "ARGORIX_RESULT:42",
      "signature": "value block needs an expected scalar type",
      "note": "`let total: u32 = loop { .. break i * 7u32; };`. The backend documents value breaks as outside the scalar C profile (core_c.rs: \"value breaks are not in scalar C profile\"), and fails earlier than that message. Recorded so the day the profile grows, the corpus says so."
    },
    {
      "id": "g19_match_bool",
      "file": "g19_match_bool.argx",
      "kind": "emit_rejected",
      "spec_expected_stdout": "ARGORIX_RESULT:40",
      "signature": "this C profile currently matches enum values only",
      "note": "`match` on a `bool`. spec/core/evaluation.md requires `match` to be exhaustive for bool and enum alike, so the bool scrutinee is specified, not an extension."
    }
  ]
}
