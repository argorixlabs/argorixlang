module conformance.policy_block

policy StrictEvidence {
    require evidence_bundle_verified
    on violation { action block trace required }
}

type Ping { content: string }
agent Worker { receives Ping on Ping as ping { trace ping } }
protocol Flow { User -> Worker: tell Ping }
