# ESP-009.F — Desbloquear la stdlib en Core: gaps del backend e imports

Estado: EN_CURSO
Revisión base: `59f70b0` (`main`, tras ESP-009.C/D/E y PR #43).
Dependencias y evidencia de cierre: ESP-008 y ESP-009.B HECHAS; corpus de gaps
g17–g23 de ESP-009.C/D (`conformance/core_c/gaps/`, issue #39); issue #45.
Subtarea de ESP-009 según la regla de subdivisión del plan maestro (§10): no
recorta ningún criterio del padre, los habilita.

## Objetivo observable

Que la stdlib de ESP-009 y el compilador de ESP-010–013 se puedan escribir en
Core sin rodeos:

1. Los siete programas de `conformance/core_c/gaps/` (g17–g23) compilan, se
   ejecutan y dan el resultado de la especificación, y pasan a `regression/`.
2. Un programa repartido en varios módulos del mismo paquete se chequea, se
   baja a IR y se ejecuta por el backend C (issue #45).

## Responsabilidad: archivos/módulos que se modificarán

- Frontend Core stage0: `crates/argorix_parser` (Core), `crates/argorix_semantics/src/core.rs`.
- IR y backend C: `crates/argorix_ir/src/core.rs`, `crates/argorix_ir/src/core_c.rs`,
  `crates/argorix_ir/tests/core_c.rs`.
- Driver: comandos Core de `crates/argorixc/src/main.rs`.
- Runtime C1: `bootstrap/c/argorix_core_runtime.{c,h}` si hace falta.
- Corpus: `conformance/core_c/**`.
- Esta ficha.

No se tocan `stdlib/**`, `spec/core/stdlib.md`, `tests/selfhost/**` ni
`tasks/espada/ESP-009.md`.

## Fuera de alcance

El issue #38 (si un `if` usado como sentencia necesita `;`) es una decisión de
lenguaje y queda para el mantenedor.

## Entradas: especificación, fixtures y contratos

- `spec/core/evaluation.md`: `loop`/`break` con valor, `match` exhaustivo
  sobre bool/enum, guardas, `_`.
- `spec/core/grammar.ebnf`: `const_decl`, `pattern`, `import_decl`.
- `spec/core/modules.md`: un módulo por archivo, símbolos privados salvo `pub`,
  grafo de imports determinista.
- Corpus g17–g23 con su resultado esperado.

## Pasos

- [ ] Gaps g17–g23
- [ ] Imports entre módulos
- [ ] Pruebas de aceptación y regresión
- [ ] Documentación y handoff
