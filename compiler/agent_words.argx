core 0.1;
module compiler.agent_words;

// The words the agent-language parser maps to enum variants (ESP-018.B).
// GENERATED by crates/argorixc/tests/agent_schema.rs from every
// `"word" => Enum::Variant` arm of crates/argorix_parser/src/parser.rs;
// regenerate with ARGORIX_BLESS=1, do not edit.
//
// `words_<function>__<enum>(word)` is the variant of the enum
// (`compiler.agent_schema`) that `word` names in that stage0 function, or
// NO_VARIANT. What stage0 does with any other word (an error, or an
// `Unknown` variant) stays in compiler/agent_parser.argx.

import stdlib.bytes;

pub const NO_VARIANT: u64 = 1000000u64;

// `parse_bridge_direction`: the `BridgeDirection` a word names.
pub const T_PARSE_BRIDGE_DIRECTION__BRIDGE_DIRECTION: u64 = 0u64;
pub fn words_parse_bridge_direction__bridge_direction(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "inbound".as_slice()) { return 0u64; }
    if bytes.equal(word, "outbound".as_slice()) { return 1u64; }
    if bytes.equal(word, "bidirectional".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_bridge_execution`: the `ATrustExecution` a word names.
pub const T_PARSE_BRIDGE_EXECUTION__A_TRUST_EXECUTION: u64 = 1u64;
pub fn words_parse_bridge_execution__a_trust_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_bridge_material`: the `ATrustMaterialBoundary` a word names.
pub const T_PARSE_BRIDGE_MATERIAL__A_TRUST_MATERIAL_BOUNDARY: u64 = 2u64;
pub fn words_parse_bridge_material__a_trust_material_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_bridge_authentication`: the `BridgeAuthentication` a word names.
pub const T_PARSE_BRIDGE_AUTHENTICATION__BRIDGE_AUTHENTICATION: u64 = 3u64;
pub fn words_parse_bridge_authentication__bridge_authentication(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_bridge_authorization`: the `BridgeAuthorization` a word names.
pub const T_PARSE_BRIDGE_AUTHORIZATION__BRIDGE_AUTHORIZATION: u64 = 4u64;
pub fn words_parse_bridge_authorization__bridge_authorization(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "policy_bound".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_governance_scope`: the `GovernanceScope` a word names.
pub const T_PARSE_GOVERNANCE_SCOPE__GOVERNANCE_SCOPE: u64 = 5u64;
pub fn words_parse_governance_scope__governance_scope(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "agent".as_slice()) { return 0u64; }
    if bytes.equal(word, "system".as_slice()) { return 1u64; }
    if bytes.equal(word, "package".as_slice()) { return 2u64; }
    if bytes.equal(word, "organization".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_governance_level`: the `GovernanceLevel` a word names.
pub const T_PARSE_GOVERNANCE_LEVEL__GOVERNANCE_LEVEL: u64 = 6u64;
pub fn words_parse_governance_level__governance_level(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "baseline".as_slice()) { return 0u64; }
    if bytes.equal(word, "audit".as_slice()) { return 1u64; }
    if bytes.equal(word, "regulated".as_slice()) { return 2u64; }
    if bytes.equal(word, "experimental".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_governance_domain`: the `GovernanceDomain` a word names.
pub const T_PARSE_GOVERNANCE_DOMAIN__GOVERNANCE_DOMAIN: u64 = 7u64;
pub fn words_parse_governance_domain__governance_domain(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "ai_agent".as_slice()) { return 0u64; }
    if bytes.equal(word, "security".as_slice()) { return 1u64; }
    if bytes.equal(word, "compliance".as_slice()) { return 2u64; }
    if bytes.equal(word, "privacy".as_slice()) { return 3u64; }
    if bytes.equal(word, "safety".as_slice()) { return 4u64; }
    if bytes.equal(word, "custom".as_slice()) { return 5u64; }
    NO_VARIANT
}

// `parse_governance_risk_level`: the `GovernanceRiskLevel` a word names.
pub const T_PARSE_GOVERNANCE_RISK_LEVEL__GOVERNANCE_RISK_LEVEL: u64 = 8u64;
pub fn words_parse_governance_risk_level__governance_risk_level(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "low".as_slice()) { return 0u64; }
    if bytes.equal(word, "moderate".as_slice()) { return 1u64; }
    if bytes.equal(word, "high".as_slice()) { return 2u64; }
    if bytes.equal(word, "critical".as_slice()) { return 3u64; }
    if bytes.equal(word, "unknown".as_slice()) { return 4u64; }
    NO_VARIANT
}

// `parse_governance_review_status`: the `GovernanceReviewStatus` a word names.
pub const T_PARSE_GOVERNANCE_REVIEW_STATUS__GOVERNANCE_REVIEW_STATUS: u64 = 9u64;
pub fn words_parse_governance_review_status__governance_review_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "draft".as_slice()) { return 0u64; }
    if bytes.equal(word, "reviewed".as_slice()) { return 1u64; }
    if bytes.equal(word, "approved_internal".as_slice()) { return 2u64; }
    if bytes.equal(word, "deprecated".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_governance_assurance`: the `GovernanceAssurance` a word names.
pub const T_PARSE_GOVERNANCE_ASSURANCE__GOVERNANCE_ASSURANCE: u64 = 10u64;
pub fn words_parse_governance_assurance__governance_assurance(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "evidence_mapped".as_slice()) { return 1u64; }
    if bytes.equal(word, "manually_reviewed".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_governance_control_category`: the `GovernanceControlCategory` a word names.
pub const T_PARSE_GOVERNANCE_CONTROL_CATEGORY__GOVERNANCE_CONTROL_CATEGORY: u64 = 11u64;
pub fn words_parse_governance_control_category__governance_control_category(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "identity".as_slice()) { return 0u64; }
    if bytes.equal(word, "credential".as_slice()) { return 1u64; }
    if bytes.equal(word, "handshake".as_slice()) { return 2u64; }
    if bytes.equal(word, "ledger".as_slice()) { return 3u64; }
    if bytes.equal(word, "bridge".as_slice()) { return 4u64; }
    if bytes.equal(word, "evidence".as_slice()) { return 5u64; }
    if bytes.equal(word, "runtime_boundary".as_slice()) { return 6u64; }
    if bytes.equal(word, "policy".as_slice()) { return 7u64; }
    if bytes.equal(word, "security".as_slice()) { return 8u64; }
    if bytes.equal(word, "privacy".as_slice()) { return 9u64; }
    if bytes.equal(word, "safety".as_slice()) { return 10u64; }
    if bytes.equal(word, "compliance".as_slice()) { return 11u64; }
    if bytes.equal(word, "custom".as_slice()) { return 12u64; }
    NO_VARIANT
}

// `parse_governance_control_status`: the `GovernanceControlStatus` a word names.
pub const T_PARSE_GOVERNANCE_CONTROL_STATUS__GOVERNANCE_CONTROL_STATUS: u64 = 12u64;
pub fn words_parse_governance_control_status__governance_control_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "mapped".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared".as_slice()) { return 1u64; }
    if bytes.equal(word, "pending_review".as_slice()) { return 2u64; }
    if bytes.equal(word, "not_applicable".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_regulatory_coverage`: the `RegulatoryCoverage` a word names.
pub const T_PARSE_REGULATORY_COVERAGE__REGULATORY_COVERAGE: u64 = 13u64;
pub fn words_parse_regulatory_coverage__regulatory_coverage(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "mapped".as_slice()) { return 0u64; }
    if bytes.equal(word, "partial".as_slice()) { return 1u64; }
    if bytes.equal(word, "pending_review".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_regulatory_assessment`: the `RegulatoryAssessment` a word names.
pub const T_PARSE_REGULATORY_ASSESSMENT__REGULATORY_ASSESSMENT: u64 = 14u64;
pub fn words_parse_regulatory_assessment__regulatory_assessment(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "evidence_mapped".as_slice()) { return 1u64; }
    if bytes.equal(word, "manual_review_required".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_regulatory_obligation_status`: the `RegulatoryObligationStatus` a word names.
pub const T_PARSE_REGULATORY_OBLIGATION_STATUS__REGULATORY_OBLIGATION_STATUS: u64 = 15u64;
pub fn words_parse_regulatory_obligation_status__regulatory_obligation_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "mapped".as_slice()) { return 0u64; }
    if bytes.equal(word, "pending_review".as_slice()) { return 1u64; }
    if bytes.equal(word, "gap".as_slice()) { return 2u64; }
    if bytes.equal(word, "not_applicable".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_third_party_verifier_type`: the `ThirdPartyVerifierType` a word names.
pub const T_PARSE_THIRD_PARTY_VERIFIER_TYPE__THIRD_PARTY_VERIFIER_TYPE: u64 = 16u64;
pub fn words_parse_third_party_verifier_type__third_party_verifier_type(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "internal".as_slice()) { return 0u64; }
    if bytes.equal(word, "community".as_slice()) { return 1u64; }
    if bytes.equal(word, "academic".as_slice()) { return 2u64; }
    if bytes.equal(word, "vendor".as_slice()) { return 3u64; }
    if bytes.equal(word, "independent_lab".as_slice()) { return 4u64; }
    if bytes.equal(word, "custom".as_slice()) { return 5u64; }
    NO_VARIANT
}

// `parse_verifier_independence`: the `VerifierIndependence` a word names.
pub const T_PARSE_VERIFIER_INDEPENDENCE__VERIFIER_INDEPENDENCE: u64 = 17u64;
pub fn words_parse_verifier_independence__verifier_independence(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared".as_slice()) { return 0u64; }
    if bytes.equal(word, "self_attested".as_slice()) { return 1u64; }
    if bytes.equal(word, "independent_declared".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_verifier_identity_mode`: the `VerifierIdentityMode` a word names.
pub const T_PARSE_VERIFIER_IDENTITY_MODE__VERIFIER_IDENTITY_MODE: u64 = 18u64;
pub fn words_parse_verifier_identity_mode__verifier_identity_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "document_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_verifier_verification_mode`: the `VerifierVerificationMode` a word names.
pub const T_PARSE_VERIFIER_VERIFICATION_MODE__VERIFIER_VERIFICATION_MODE: u64 = 19u64;
pub fn words_parse_verifier_verification_mode__verifier_verification_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "reproducible_artifacts".as_slice()) { return 0u64; }
    if bytes.equal(word, "document_review".as_slice()) { return 1u64; }
    if bytes.equal(word, "conformance_replay".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_public_conformance_result`: the `PublicConformanceResult` a word names.
pub const T_PARSE_PUBLIC_CONFORMANCE_RESULT__PUBLIC_CONFORMANCE_RESULT: u64 = 20u64;
pub fn words_parse_public_conformance_result__public_conformance_result(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "passed".as_slice()) { return 0u64; }
    if bytes.equal(word, "failed".as_slice()) { return 1u64; }
    if bytes.equal(word, "pending_review".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_public_conformance_reproducibility`: the `PublicConformanceReproducibility` a word names.
pub const T_PARSE_PUBLIC_CONFORMANCE_REPRODUCIBILITY__PUBLIC_CONFORMANCE_REPRODUCIBILITY: u64 = 21u64;
pub fn words_parse_public_conformance_reproducibility__public_conformance_reproducibility(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared".as_slice()) { return 0u64; }
    if bytes.equal(word, "replayable_locally".as_slice()) { return 1u64; }
    if bytes.equal(word, "document_only".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_public_conformance_review_status`: the `PublicConformanceReviewStatus` a word names.
pub const T_PARSE_PUBLIC_CONFORMANCE_REVIEW_STATUS__PUBLIC_CONFORMANCE_REVIEW_STATUS: u64 = 22u64;
pub fn words_parse_public_conformance_review_status__public_conformance_review_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "draft".as_slice()) { return 0u64; }
    if bytes.equal(word, "reviewed".as_slice()) { return 1u64; }
    if bytes.equal(word, "published".as_slice()) { return 2u64; }
    if bytes.equal(word, "deprecated".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_public_conformance_claim_category`: the `PublicConformanceClaimCategory` a word names.
pub const T_PARSE_PUBLIC_CONFORMANCE_CLAIM_CATEGORY__PUBLIC_CONFORMANCE_CLAIM_CATEGORY: u64 = 23u64;
pub fn words_parse_public_conformance_claim_category__public_conformance_claim_category(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "conformance".as_slice()) { return 0u64; }
    if bytes.equal(word, "evidence".as_slice()) { return 1u64; }
    if bytes.equal(word, "security_report".as_slice()) { return 2u64; }
    if bytes.equal(word, "governance".as_slice()) { return 3u64; }
    if bytes.equal(word, "regulatory_mapping".as_slice()) { return 4u64; }
    if bytes.equal(word, "bytecode".as_slice()) { return 5u64; }
    if bytes.equal(word, "source".as_slice()) { return 6u64; }
    if bytes.equal(word, "policy".as_slice()) { return 7u64; }
    if bytes.equal(word, "runtime_boundary".as_slice()) { return 8u64; }
    if bytes.equal(word, "custom".as_slice()) { return 9u64; }
    NO_VARIANT
}

// `parse_public_conformance_claim_status`: the `PublicConformanceClaimStatus` a word names.
pub const T_PARSE_PUBLIC_CONFORMANCE_CLAIM_STATUS__PUBLIC_CONFORMANCE_CLAIM_STATUS: u64 = 24u64;
pub fn words_parse_public_conformance_claim_status__public_conformance_claim_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "mapped".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared".as_slice()) { return 1u64; }
    if bytes.equal(word, "pending_review".as_slice()) { return 2u64; }
    if bytes.equal(word, "not_applicable".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_governance_network`: the `ATrustNetworkBoundary` a word names.
pub const T_PARSE_GOVERNANCE_NETWORK__A_TRUST_NETWORK_BOUNDARY: u64 = 25u64;
pub fn words_parse_governance_network__a_trust_network_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_governance_security_claims`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_GOVERNANCE_SECURITY_CLAIMS__A_TRUST_SECURITY_CLAIMS: u64 = 26u64;
pub fn words_parse_governance_security_claims__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_provider`: the `ProviderKindDecl` a word names.
pub const T_PARSE_PROVIDER__PROVIDER_KIND_DECL: u64 = 27u64;
pub fn words_parse_provider__provider_kind_decl(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "simulated".as_slice()) { return 0u64; }
    if bytes.equal(word, "external".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_harness`: the `HarnessMode` a word names.
pub const T_PARSE_HARNESS__HARNESS_MODE: u64 = 28u64;
pub fn words_parse_harness__harness_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "dry_run".as_slice()) { return 0u64; }
    if bytes.equal(word, "simulated".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_harness`: the `HarnessNetwork` a word names.
pub const T_PARSE_HARNESS__HARNESS_NETWORK: u64 = 29u64;
pub fn words_parse_harness__harness_network(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_harness`: the `HarnessSecrets` a word names.
pub const T_PARSE_HARNESS__HARNESS_SECRETS: u64 = 30u64;
pub fn words_parse_harness__harness_secrets(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_harness`: the `HarnessFilesystem` a word names.
pub const T_PARSE_HARNESS__HARNESS_FILESYSTEM: u64 = 31u64;
pub fn words_parse_harness__harness_filesystem(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    if bytes.equal(word, "read_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_feature`: the `FeatureStatus` a word names.
pub const T_PARSE_FEATURE__FEATURE_STATUS: u64 = 32u64;
pub fn words_parse_feature__feature_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "experimental".as_slice()) { return 0u64; }
    if bytes.equal(word, "preview".as_slice()) { return 1u64; }
    if bytes.equal(word, "stable".as_slice()) { return 2u64; }
    if bytes.equal(word, "deprecated".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_feature`: the `FeatureDefault` a word names.
pub const T_PARSE_FEATURE__FEATURE_DEFAULT: u64 = 33u64;
pub fn words_parse_feature__feature_default(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "enabled".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_secret`: the `SecretScope` a word names.
pub const T_PARSE_SECRET__SECRET_SCOPE: u64 = 34u64;
pub fn words_parse_secret__secret_scope(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "provider".as_slice()) { return 0u64; }
    if bytes.equal(word, "adapter".as_slice()) { return 1u64; }
    if bytes.equal(word, "model".as_slice()) { return 2u64; }
    if bytes.equal(word, "tool".as_slice()) { return 3u64; }
    if bytes.equal(word, "runtime".as_slice()) { return 4u64; }
    NO_VARIANT
}

// `parse_secret`: the `SecretAccess` a word names.
pub const T_PARSE_SECRET__SECRET_ACCESS: u64 = 35u64;
pub fn words_parse_secret__secret_access(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_secret`: the `SecretSource` a word names.
pub const T_PARSE_SECRET__SECRET_SOURCE: u64 = 36u64;
pub fn words_parse_secret__secret_source(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_adapter`: the `AdapterKind` a word names.
pub const T_PARSE_ADAPTER__ADAPTER_KIND: u64 = 37u64;
pub fn words_parse_adapter__adapter_kind(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "llm".as_slice()) { return 0u64; }
    if bytes.equal(word, "tool".as_slice()) { return 1u64; }
    if bytes.equal(word, "bridge".as_slice()) { return 2u64; }
    if bytes.equal(word, "registry".as_slice()) { return 3u64; }
    if bytes.equal(word, "identity".as_slice()) { return 4u64; }
    if bytes.equal(word, "payment".as_slice()) { return 5u64; }
    if bytes.equal(word, "storage".as_slice()) { return 6u64; }
    if bytes.equal(word, "custom".as_slice()) { return 7u64; }
    NO_VARIANT
}

// `parse_adapter`: the `AdapterMode` a word names.
pub const T_PARSE_ADAPTER__ADAPTER_MODE: u64 = 38u64;
pub fn words_parse_adapter__adapter_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "experimental".as_slice()) { return 0u64; }
    if bytes.equal(word, "preview".as_slice()) { return 1u64; }
    if bytes.equal(word, "stable".as_slice()) { return 2u64; }
    if bytes.equal(word, "deprecated".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_adapter`: the `AdapterExecution` a word names.
pub const T_PARSE_ADAPTER__ADAPTER_EXECUTION: u64 = 39u64;
pub fn words_parse_adapter__adapter_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_adapter`: the `AdapterNetwork` a word names.
pub const T_PARSE_ADAPTER__ADAPTER_NETWORK: u64 = 40u64;
pub fn words_parse_adapter__adapter_network(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_adapter`: the `AdapterSecrets` a word names.
pub const T_PARSE_ADAPTER__ADAPTER_SECRETS: u64 = 41u64;
pub fn words_parse_adapter__adapter_secrets(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_adapter`: the `AdapterFilesystem` a word names.
pub const T_PARSE_ADAPTER__ADAPTER_FILESYSTEM: u64 = 42u64;
pub fn words_parse_adapter__adapter_filesystem(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    if bytes.equal(word, "read_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_adapter_profile`: the `AdapterProfileFamily` a word names.
pub const T_PARSE_ADAPTER_PROFILE__ADAPTER_PROFILE_FAMILY: u64 = 43u64;
pub fn words_parse_adapter_profile__adapter_profile_family(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "llm".as_slice()) { return 0u64; }
    if bytes.equal(word, "tool".as_slice()) { return 1u64; }
    if bytes.equal(word, "bridge".as_slice()) { return 2u64; }
    if bytes.equal(word, "registry".as_slice()) { return 3u64; }
    if bytes.equal(word, "identity".as_slice()) { return 4u64; }
    if bytes.equal(word, "payment".as_slice()) { return 5u64; }
    if bytes.equal(word, "storage".as_slice()) { return 6u64; }
    if bytes.equal(word, "custom".as_slice()) { return 7u64; }
    NO_VARIANT
}

// `parse_adapter_profile`: the `AdapterProfileApiStyle` a word names.
pub const T_PARSE_ADAPTER_PROFILE__ADAPTER_PROFILE_API_STYLE: u64 = 44u64;
pub fn words_parse_adapter_profile__adapter_profile_api_style(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "responses".as_slice()) { return 0u64; }
    if bytes.equal(word, "messages".as_slice()) { return 1u64; }
    if bytes.equal(word, "chat".as_slice()) { return 2u64; }
    if bytes.equal(word, "completion".as_slice()) { return 3u64; }
    if bytes.equal(word, "tool_call".as_slice()) { return 4u64; }
    if bytes.equal(word, "mcp".as_slice()) { return 5u64; }
    if bytes.equal(word, "a2a".as_slice()) { return 6u64; }
    if bytes.equal(word, "rest".as_slice()) { return 7u64; }
    if bytes.equal(word, "custom".as_slice()) { return 8u64; }
    NO_VARIANT
}

// `parse_adapter_profile`: the `AdapterProfileAuth` a word names.
pub const T_PARSE_ADAPTER_PROFILE__ADAPTER_PROFILE_AUTH: u64 = 45u64;
pub fn words_parse_adapter_profile__adapter_profile_auth(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    if bytes.equal(word, "secret_boundary".as_slice()) { return 1u64; }
    if bytes.equal(word, "did".as_slice()) { return 2u64; }
    if bytes.equal(word, "credential".as_slice()) { return 3u64; }
    if bytes.equal(word, "custom".as_slice()) { return 4u64; }
    NO_VARIANT
}

// `parse_adapter_profile`: the `AdapterProfileExecution` a word names.
pub const T_PARSE_ADAPTER_PROFILE__ADAPTER_PROFILE_EXECUTION: u64 = 46u64;
pub fn words_parse_adapter_profile__adapter_profile_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_adapter_profile`: the `AdapterProfileNetwork` a word names.
pub const T_PARSE_ADAPTER_PROFILE__ADAPTER_PROFILE_NETWORK: u64 = 47u64;
pub fn words_parse_adapter_profile__adapter_profile_network(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_adapter_profile`: the `AdapterProfileSecrets` a word names.
pub const T_PARSE_ADAPTER_PROFILE__ADAPTER_PROFILE_SECRETS: u64 = 48u64;
pub fn words_parse_adapter_profile__adapter_profile_secrets(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_crypto`: the `CryptoKind` a word names.
pub const T_PARSE_CRYPTO__CRYPTO_KIND: u64 = 49u64;
pub fn words_parse_crypto__crypto_kind(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "hash".as_slice()) { return 0u64; }
    if bytes.equal(word, "signature".as_slice()) { return 1u64; }
    if bytes.equal(word, "kem".as_slice()) { return 2u64; }
    if bytes.equal(word, "aead".as_slice()) { return 3u64; }
    if bytes.equal(word, "mac".as_slice()) { return 4u64; }
    if bytes.equal(word, "kdf".as_slice()) { return 5u64; }
    if bytes.equal(word, "commitment".as_slice()) { return 6u64; }
    if bytes.equal(word, "randomness".as_slice()) { return 7u64; }
    if bytes.equal(word, "custom".as_slice()) { return 8u64; }
    NO_VARIANT
}

// `parse_crypto`: the `CryptoStatus` a word names.
pub const T_PARSE_CRYPTO__CRYPTO_STATUS: u64 = 50u64;
pub fn words_parse_crypto__crypto_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "allowed".as_slice()) { return 0u64; }
    if bytes.equal(word, "legacy".as_slice()) { return 1u64; }
    if bytes.equal(word, "deprecated".as_slice()) { return 2u64; }
    if bytes.equal(word, "denied".as_slice()) { return 3u64; }
    if bytes.equal(word, "experimental".as_slice()) { return 4u64; }
    if bytes.equal(word, "post_quantum_candidate".as_slice()) { return 5u64; }
    NO_VARIANT
}

// `parse_crypto`: the `CryptoStrength` a word names.
pub const T_PARSE_CRYPTO__CRYPTO_STRENGTH: u64 = 51u64;
pub fn words_parse_crypto__crypto_strength(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "classical".as_slice()) { return 0u64; }
    if bytes.equal(word, "post_quantum".as_slice()) { return 1u64; }
    if bytes.equal(word, "hybrid".as_slice()) { return 2u64; }
    if bytes.equal(word, "unknown".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_did_method`: the `DidMethodStatus` a word names.
pub const T_PARSE_DID_METHOD__DID_METHOD_STATUS: u64 = 52u64;
pub fn words_parse_did_method__did_method_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "experimental".as_slice()) { return 0u64; }
    if bytes.equal(word, "preview".as_slice()) { return 1u64; }
    if bytes.equal(word, "stable".as_slice()) { return 2u64; }
    if bytes.equal(word, "deprecated".as_slice()) { return 3u64; }
    if bytes.equal(word, "denied".as_slice()) { return 4u64; }
    NO_VARIANT
}

// `parse_did_method`: the `DidResolutionMode` a word names.
pub const T_PARSE_DID_METHOD__DID_RESOLUTION_MODE: u64 = 53u64;
pub fn words_parse_did_method__did_resolution_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "embedded".as_slice()) { return 1u64; }
    if bytes.equal(word, "local".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_did_method`: the `DidLedgerMode` a word names.
pub const T_PARSE_DID_METHOD__DID_LEDGER_MODE: u64 = 54u64;
pub fn words_parse_did_method__did_ledger_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    if bytes.equal(word, "local".as_slice()) { return 1u64; }
    if bytes.equal(word, "embedded".as_slice()) { return 2u64; }
    if bytes.equal(word, "custom".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustIdentityFormat` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_IDENTITY_FORMAT: u64 = 55u64;
pub fn words_parse_atrust_boundary__a_trust_identity_format(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "did".as_slice()) { return 0u64; }
    if bytes.equal(word, "opaque".as_slice()) { return 1u64; }
    if bytes.equal(word, "custom".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustCredentialMode` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_CREDENTIAL_MODE: u64 = 56u64;
pub fn words_parse_atrust_boundary__a_trust_credential_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustHandshakeMode` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_HANDSHAKE_MODE: u64 = 57u64;
pub fn words_parse_atrust_boundary__a_trust_handshake_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustResolutionMode` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_RESOLUTION_MODE: u64 = 58u64;
pub fn words_parse_atrust_boundary__a_trust_resolution_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "embedded".as_slice()) { return 1u64; }
    if bytes.equal(word, "local".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustMaterialBoundary` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_MATERIAL_BOUNDARY: u64 = 59u64;
pub fn words_parse_atrust_boundary__a_trust_material_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustExecution` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_EXECUTION: u64 = 60u64;
pub fn words_parse_atrust_boundary__a_trust_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustPostQuantumRequirement` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_POST_QUANTUM_REQUIREMENT: u64 = 61u64;
pub fn words_parse_atrust_boundary__a_trust_post_quantum_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    if bytes.equal(word, "optional".as_slice()) { return 1u64; }
    if bytes.equal(word, "not_required".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_boundary`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_ATRUST_BOUNDARY__A_TRUST_SECURITY_CLAIMS: u64 = 62u64;
pub fn words_parse_atrust_boundary__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_identity`: the `ATrustIdentityStatus` a word names.
pub const T_PARSE_ATRUST_IDENTITY__A_TRUST_IDENTITY_STATUS: u64 = 63u64;
pub fn words_parse_atrust_identity__a_trust_identity_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "active".as_slice()) { return 0u64; }
    if bytes.equal(word, "suspended".as_slice()) { return 1u64; }
    if bytes.equal(word, "revoked".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_identity`: the `ATrustIdentityValidation` a word names.
pub const T_PARSE_ATRUST_IDENTITY__A_TRUST_IDENTITY_VALIDATION: u64 = 64u64;
pub fn words_parse_atrust_identity__a_trust_identity_validation(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "dry_run".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_identity`: the `ATrustResolutionMode` a word names.
pub const T_PARSE_ATRUST_IDENTITY__A_TRUST_RESOLUTION_MODE: u64 = 65u64;
pub fn words_parse_atrust_identity__a_trust_resolution_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "embedded".as_slice()) { return 1u64; }
    if bytes.equal(word, "local".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_identity`: the `ATrustMaterialBoundary` a word names.
pub const T_PARSE_ATRUST_IDENTITY__A_TRUST_MATERIAL_BOUNDARY: u64 = 66u64;
pub fn words_parse_atrust_identity__a_trust_material_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_identity`: the `ATrustExecution` a word names.
pub const T_PARSE_ATRUST_IDENTITY__A_TRUST_EXECUTION: u64 = 67u64;
pub fn words_parse_atrust_identity__a_trust_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_identity`: the `ATrustEvidenceRequirement` a word names.
pub const T_PARSE_ATRUST_IDENTITY__A_TRUST_EVIDENCE_REQUIREMENT: u64 = 68u64;
pub fn words_parse_atrust_identity__a_trust_evidence_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_identity`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_ATRUST_IDENTITY__A_TRUST_SECURITY_CLAIMS: u64 = 69u64;
pub fn words_parse_atrust_identity__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustCredentialStatus` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_CREDENTIAL_STATUS: u64 = 70u64;
pub fn words_parse_atrust_credential_contract__a_trust_credential_status(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared".as_slice()) { return 0u64; }
    if bytes.equal(word, "active".as_slice()) { return 1u64; }
    if bytes.equal(word, "suspended".as_slice()) { return 2u64; }
    if bytes.equal(word, "revoked".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustCredentialVerification` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_CREDENTIAL_VERIFICATION: u64 = 71u64;
pub fn words_parse_atrust_credential_contract__a_trust_credential_verification(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustCredentialPresentation` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_CREDENTIAL_PRESENTATION: u64 = 72u64;
pub fn words_parse_atrust_credential_contract__a_trust_credential_presentation(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustResolutionMode` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_RESOLUTION_MODE: u64 = 73u64;
pub fn words_parse_atrust_credential_contract__a_trust_resolution_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "embedded".as_slice()) { return 1u64; }
    if bytes.equal(word, "local".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustMaterialBoundary` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_MATERIAL_BOUNDARY: u64 = 74u64;
pub fn words_parse_atrust_credential_contract__a_trust_material_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustExecution` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_EXECUTION: u64 = 75u64;
pub fn words_parse_atrust_credential_contract__a_trust_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustEvidenceRequirement` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_EVIDENCE_REQUIREMENT: u64 = 76u64;
pub fn words_parse_atrust_credential_contract__a_trust_evidence_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_credential_contract`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_ATRUST_CREDENTIAL_CONTRACT__A_TRUST_SECURITY_CLAIMS: u64 = 77u64;
pub fn words_parse_atrust_credential_contract__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustHandshakeDryRunMode` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_HANDSHAKE_DRY_RUN_MODE: u64 = 78u64;
pub fn words_parse_atrust_handshake__a_trust_handshake_dry_run_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "dry_run".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustHandshakeDirection` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_HANDSHAKE_DIRECTION: u64 = 79u64;
pub fn words_parse_atrust_handshake__a_trust_handshake_direction(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "one_way".as_slice()) { return 0u64; }
    if bytes.equal(word, "mutual".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustHandshakeChallenge` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_HANDSHAKE_CHALLENGE: u64 = 80u64;
pub fn words_parse_atrust_handshake__a_trust_handshake_challenge(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustHandshakeResponse` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_HANDSHAKE_RESPONSE: u64 = 81u64;
pub fn words_parse_atrust_handshake__a_trust_handshake_response(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustHandshakeTranscript` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_HANDSHAKE_TRANSCRIPT: u64 = 82u64;
pub fn words_parse_atrust_handshake__a_trust_handshake_transcript(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "metadata_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "evidence_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustHandshakeVerification` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_HANDSHAKE_VERIFICATION: u64 = 83u64;
pub fn words_parse_atrust_handshake__a_trust_handshake_verification(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustResolutionMode` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_RESOLUTION_MODE: u64 = 84u64;
pub fn words_parse_atrust_handshake__a_trust_resolution_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "embedded".as_slice()) { return 1u64; }
    if bytes.equal(word, "local".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustNetworkBoundary` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_NETWORK_BOUNDARY: u64 = 85u64;
pub fn words_parse_atrust_handshake__a_trust_network_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustMaterialBoundary` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_MATERIAL_BOUNDARY: u64 = 86u64;
pub fn words_parse_atrust_handshake__a_trust_material_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustExecution` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_EXECUTION: u64 = 87u64;
pub fn words_parse_atrust_handshake__a_trust_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustEvidenceRequirement` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_EVIDENCE_REQUIREMENT: u64 = 88u64;
pub fn words_parse_atrust_handshake__a_trust_evidence_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_handshake`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_ATRUST_HANDSHAKE__A_TRUST_SECURITY_CLAIMS: u64 = 89u64;
pub fn words_parse_atrust_handshake__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `TrustLedgerScope` a word names.
pub const T_PARSE_TRUST_LEDGER__TRUST_LEDGER_SCOPE: u64 = 90u64;
pub fn words_parse_trust_ledger__trust_ledger_scope(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "local".as_slice()) { return 0u64; }
    if bytes.equal(word, "package".as_slice()) { return 1u64; }
    if bytes.equal(word, "bundle".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `TrustLedgerMode` a word names.
pub const T_PARSE_TRUST_LEDGER__TRUST_LEDGER_MODE: u64 = 91u64;
pub fn words_parse_trust_ledger__trust_ledger_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "dry_run".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `TrustLedgerChainPolicy` a word names.
pub const T_PARSE_TRUST_LEDGER__TRUST_LEDGER_CHAIN_POLICY: u64 = 92u64;
pub fn words_parse_trust_ledger__trust_ledger_chain_policy(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "append_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `ATrustNetworkBoundary` a word names.
pub const T_PARSE_TRUST_LEDGER__A_TRUST_NETWORK_BOUNDARY: u64 = 93u64;
pub fn words_parse_trust_ledger__a_trust_network_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `ATrustMaterialBoundary` a word names.
pub const T_PARSE_TRUST_LEDGER__A_TRUST_MATERIAL_BOUNDARY: u64 = 94u64;
pub fn words_parse_trust_ledger__a_trust_material_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `ATrustExecution` a word names.
pub const T_PARSE_TRUST_LEDGER__A_TRUST_EXECUTION: u64 = 95u64;
pub fn words_parse_trust_ledger__a_trust_execution(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `ATrustEvidenceRequirement` a word names.
pub const T_PARSE_TRUST_LEDGER__A_TRUST_EVIDENCE_REQUIREMENT: u64 = 96u64;
pub fn words_parse_trust_ledger__a_trust_evidence_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_trust_ledger`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_TRUST_LEDGER__A_TRUST_SECURITY_CLAIMS: u64 = 97u64;
pub fn words_parse_trust_ledger__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_trust_ledger_entry`: the `TrustLedgerEntryKind` a word names.
pub const T_PARSE_TRUST_LEDGER_ENTRY__TRUST_LEDGER_ENTRY_KIND: u64 = 98u64;
pub fn words_parse_trust_ledger_entry__trust_ledger_entry_kind(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "identity".as_slice()) { return 0u64; }
    if bytes.equal(word, "credential".as_slice()) { return 1u64; }
    if bytes.equal(word, "handshake".as_slice()) { return 2u64; }
    if bytes.equal(word, "evidence".as_slice()) { return 3u64; }
    if bytes.equal(word, "policy".as_slice()) { return 4u64; }
    if bytes.equal(word, "custom".as_slice()) { return 5u64; }
    NO_VARIANT
}

// `parse_mcp_bridge_contract`: the `BridgeTransport` a word names.
pub const T_PARSE_MCP_BRIDGE_CONTRACT__BRIDGE_TRANSPORT: u64 = 99u64;
pub fn words_parse_mcp_bridge_contract__bridge_transport(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "disabled".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_mcp_bridge_contract`: the `McpProtocol` a word names.
pub const T_PARSE_MCP_BRIDGE_CONTRACT__MCP_PROTOCOL: u64 = 100u64;
pub fn words_parse_mcp_bridge_contract__mcp_protocol(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "mcp".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_mcp_bridge_contract`: the `ATrustNetworkBoundary` a word names.
pub const T_PARSE_MCP_BRIDGE_CONTRACT__A_TRUST_NETWORK_BOUNDARY: u64 = 101u64;
pub fn words_parse_mcp_bridge_contract__a_trust_network_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_mcp_bridge_contract`: the `ATrustEvidenceRequirement` a word names.
pub const T_PARSE_MCP_BRIDGE_CONTRACT__A_TRUST_EVIDENCE_REQUIREMENT: u64 = 102u64;
pub fn words_parse_mcp_bridge_contract__a_trust_evidence_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_mcp_bridge_contract`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_MCP_BRIDGE_CONTRACT__A_TRUST_SECURITY_CLAIMS: u64 = 103u64;
pub fn words_parse_mcp_bridge_contract__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_a2a_bridge_contract`: the `A2AProtocol` a word names.
pub const T_PARSE_A2A_BRIDGE_CONTRACT__A2_A_PROTOCOL: u64 = 104u64;
pub fn words_parse_a2a_bridge_contract__a2_a_protocol(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "a2a".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_a2a_bridge_contract`: the `BridgeTransport` a word names.
pub const T_PARSE_A2A_BRIDGE_CONTRACT__BRIDGE_TRANSPORT: u64 = 105u64;
pub fn words_parse_a2a_bridge_contract__bridge_transport(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "disabled".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_a2a_bridge_contract`: the `ATrustNetworkBoundary` a word names.
pub const T_PARSE_A2A_BRIDGE_CONTRACT__A_TRUST_NETWORK_BOUNDARY: u64 = 106u64;
pub fn words_parse_a2a_bridge_contract__a_trust_network_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_a2a_bridge_contract`: the `ATrustEvidenceRequirement` a word names.
pub const T_PARSE_A2A_BRIDGE_CONTRACT__A_TRUST_EVIDENCE_REQUIREMENT: u64 = 107u64;
pub fn words_parse_a2a_bridge_contract__a_trust_evidence_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_a2a_bridge_contract`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_A2A_BRIDGE_CONTRACT__A_TRUST_SECURITY_CLAIMS: u64 = 108u64;
pub fn words_parse_a2a_bridge_contract__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_evidence_map`: the `ATrustEvidenceMapCoverage` a word names.
pub const T_PARSE_ATRUST_EVIDENCE_MAP__A_TRUST_EVIDENCE_MAP_COVERAGE: u64 = 109u64;
pub fn words_parse_atrust_evidence_map__a_trust_evidence_map_coverage(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    if bytes.equal(word, "complete".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_evidence_map`: the `ATrustEvidenceMapMappingMode` a word names.
pub const T_PARSE_ATRUST_EVIDENCE_MAP__A_TRUST_EVIDENCE_MAP_MAPPING_MODE: u64 = 110u64;
pub fn words_parse_atrust_evidence_map__a_trust_evidence_map_mapping_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 0u64; }
    if bytes.equal(word, "evidence_only".as_slice()) { return 1u64; }
    NO_VARIANT
}

// `parse_atrust_evidence_map`: the `ATrustHandshakeVerification` a word names.
pub const T_PARSE_ATRUST_EVIDENCE_MAP__A_TRUST_HANDSHAKE_VERIFICATION: u64 = 111u64;
pub fn words_parse_atrust_evidence_map__a_trust_handshake_verification(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "declared_only".as_slice()) { return 1u64; }
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_evidence_map`: the `ATrustResolutionMode` a word names.
pub const T_PARSE_ATRUST_EVIDENCE_MAP__A_TRUST_RESOLUTION_MODE: u64 = 112u64;
pub fn words_parse_atrust_evidence_map__a_trust_resolution_mode(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "disabled".as_slice()) { return 0u64; }
    if bytes.equal(word, "embedded".as_slice()) { return 1u64; }
    if bytes.equal(word, "local".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_atrust_evidence_map`: the `ATrustEvidenceRequirement` a word names.
pub const T_PARSE_ATRUST_EVIDENCE_MAP__A_TRUST_EVIDENCE_REQUIREMENT: u64 = 113u64;
pub fn words_parse_atrust_evidence_map__a_trust_evidence_requirement(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "required".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_evidence_map`: the `ATrustNetworkBoundary` a word names.
pub const T_PARSE_ATRUST_EVIDENCE_MAP__A_TRUST_NETWORK_BOUNDARY: u64 = 114u64;
pub fn words_parse_atrust_evidence_map__a_trust_network_boundary(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "denied".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_atrust_evidence_map`: the `ATrustSecurityClaims` a word names.
pub const T_PARSE_ATRUST_EVIDENCE_MAP__A_TRUST_SECURITY_CLAIMS: u64 = 115u64;
pub fn words_parse_atrust_evidence_map__a_trust_security_claims(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "none".as_slice()) { return 0u64; }
    NO_VARIANT
}

// `parse_policy_rule`: the `PolicyRule` a word names.
pub const T_PARSE_POLICY_RULE__POLICY_RULE: u64 = 116u64;
pub fn words_parse_policy_rule__policy_rule(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "no_unhandled_messages".as_slice()) { return 0u64; }
    if bytes.equal(word, "all_tool_calls_traced".as_slice()) { return 1u64; }
    if bytes.equal(word, "all_model_calls_traced".as_slice()) { return 2u64; }
    if bytes.equal(word, "all_intrinsics_traced".as_slice()) { return 3u64; }
    if bytes.equal(word, "all_provider_calls_traced".as_slice()) { return 4u64; }
    if bytes.equal(word, "halt_requires_trace".as_slice()) { return 5u64; }
    if bytes.equal(word, "provider_contracts_declared".as_slice()) { return 7u64; }
    if bytes.equal(word, "provider_allowlists_valid".as_slice()) { return 8u64; }
    if bytes.equal(word, "external_execution".as_slice()) { return 9u64; }
    if bytes.equal(word, "evidence_bundle_verified".as_slice()) { return 10u64; }
    if bytes.equal(word, "security_report_generated".as_slice()) { return 11u64; }
    if bytes.equal(word, "agent_passport_declared".as_slice()) { return 12u64; }
    if bytes.equal(word, "agent_passport_attested".as_slice()) { return 13u64; }
    if bytes.equal(word, "agent_data_residency_declared".as_slice()) { return 14u64; }
    if bytes.equal(word, "agent_identity_declared".as_slice()) { return 15u64; }
    if bytes.equal(word, "provider_harness_declared".as_slice()) { return 16u64; }
    if bytes.equal(word, "provider_harness_sandboxed".as_slice()) { return 17u64; }
    if bytes.equal(word, "provider_network_denied".as_slice()) { return 18u64; }
    if bytes.equal(word, "provider_secrets_denied".as_slice()) { return 19u64; }
    if bytes.equal(word, "provider_filesystem_restricted".as_slice()) { return 20u64; }
    if bytes.equal(word, "external_provider_harnessed".as_slice()) { return 21u64; }
    if bytes.equal(word, "feature_flags_declared".as_slice()) { return 22u64; }
    if bytes.equal(word, "features_default_disabled".as_slice()) { return 23u64; }
    if bytes.equal(word, "experimental_features_require_approval".as_slice()) { return 24u64; }
    if bytes.equal(word, "secret_boundaries_declared".as_slice()) { return 25u64; }
    if bytes.equal(word, "secret_access_denied".as_slice()) { return 26u64; }
    if bytes.equal(word, "secret_values_absent".as_slice()) { return 27u64; }
    if bytes.equal(word, "external_provider_feature_gated".as_slice()) { return 28u64; }
    if bytes.equal(word, "external_provider_secret_boundary_declared".as_slice()) { return 29u64; }
    if bytes.equal(word, "adapters_declared".as_slice()) { return 30u64; }
    if bytes.equal(word, "adapters_execution_disabled".as_slice()) { return 31u64; }
    if bytes.equal(word, "adapters_network_denied".as_slice()) { return 32u64; }
    if bytes.equal(word, "adapters_secrets_denied".as_slice()) { return 33u64; }
    if bytes.equal(word, "adapters_provider_harnessed".as_slice()) { return 34u64; }
    if bytes.equal(word, "adapters_feature_gated".as_slice()) { return 35u64; }
    if bytes.equal(word, "adapters_secret_boundaried".as_slice()) { return 36u64; }
    if bytes.equal(word, "adapters_conformance_declared".as_slice()) { return 37u64; }
    if bytes.equal(word, "adapters_evidence_required".as_slice()) { return 38u64; }
    if bytes.equal(word, "adapter_profiles_declared".as_slice()) { return 39u64; }
    if bytes.equal(word, "adapter_profiles_execution_disabled".as_slice()) { return 40u64; }
    if bytes.equal(word, "adapter_profiles_network_denied".as_slice()) { return 41u64; }
    if bytes.equal(word, "adapter_profiles_secrets_denied".as_slice()) { return 42u64; }
    if bytes.equal(word, "adapter_profiles_linked".as_slice()) { return 43u64; }
    if bytes.equal(word, "adapter_profiles_conformance_declared".as_slice()) { return 44u64; }
    if bytes.equal(word, "vendor_profiles_declared".as_slice()) { return 45u64; }
    if bytes.equal(word, "crypto_primitives_declared".as_slice()) { return 46u64; }
    if bytes.equal(word, "crypto_primitives_allowed".as_slice()) { return 47u64; }
    if bytes.equal(word, "crypto_denied_not_used".as_slice()) { return 48u64; }
    if bytes.equal(word, "crypto_post_quantum_candidates_declared".as_slice()) { return 49u64; }
    if bytes.equal(word, "crypto_key_material_absent".as_slice()) { return 50u64; }
    if bytes.equal(word, "crypto_secret_material_absent".as_slice()) { return 51u64; }
    if bytes.equal(word, "crypto_execution_absent".as_slice()) { return 52u64; }
    if bytes.equal(word, "crypto_boundaries_declared".as_slice()) { return 53u64; }
    if bytes.equal(word, "post_quantum_readiness_declared".as_slice()) { return 54u64; }
    if bytes.equal(word, "atrust_boundaries_declared".as_slice()) { return 55u64; }
    if bytes.equal(word, "atrust_did_methods_declared".as_slice()) { return 56u64; }
    if bytes.equal(word, "atrust_did_method_allowed".as_slice()) { return 57u64; }
    if bytes.equal(word, "atrust_identity_format_declared".as_slice()) { return 58u64; }
    if bytes.equal(word, "atrust_credential_mode_declared".as_slice()) { return 59u64; }
    if bytes.equal(word, "atrust_handshake_disabled".as_slice()) { return 60u64; }
    if bytes.equal(word, "atrust_resolution_disabled".as_slice()) { return 61u64; }
    if bytes.equal(word, "atrust_key_material_denied".as_slice()) { return 62u64; }
    if bytes.equal(word, "atrust_secret_material_denied".as_slice()) { return 63u64; }
    if bytes.equal(word, "atrust_execution_disabled".as_slice()) { return 64u64; }
    if bytes.equal(word, "atrust_post_quantum_readiness_declared".as_slice()) { return 65u64; }
    if bytes.equal(word, "atrust_security_claims_none".as_slice()) { return 66u64; }
    if bytes.equal(word, "atrust_identity_declared".as_slice()) { return 67u64; }
    if bytes.equal(word, "atrust_identity_subject_valid".as_slice()) { return 68u64; }
    if bytes.equal(word, "atrust_identity_did_method_valid".as_slice()) { return 69u64; }
    if bytes.equal(word, "atrust_identity_boundary_valid".as_slice()) { return 70u64; }
    if bytes.equal(word, "atrust_identity_status_active".as_slice()) { return 71u64; }
    if bytes.equal(word, "atrust_identity_validation_dry_run".as_slice()) { return 72u64; }
    if bytes.equal(word, "atrust_identity_resolution_disabled".as_slice()) { return 73u64; }
    if bytes.equal(word, "atrust_identity_key_material_denied".as_slice()) { return 74u64; }
    if bytes.equal(word, "atrust_identity_secret_material_denied".as_slice()) { return 75u64; }
    if bytes.equal(word, "atrust_identity_execution_disabled".as_slice()) { return 76u64; }
    if bytes.equal(word, "atrust_identity_evidence_required".as_slice()) { return 77u64; }
    if bytes.equal(word, "atrust_identity_security_claims_absent".as_slice()) { return 78u64; }
    if bytes.equal(word, "atrust_identity_passport_consistent".as_slice()) { return 79u64; }
    if bytes.equal(word, "atrust_credential_contract_declared".as_slice()) { return 80u64; }
    if bytes.equal(word, "atrust_credential_issuer_did_declared".as_slice()) { return 81u64; }
    if bytes.equal(word, "atrust_credential_holder_did_declared".as_slice()) { return 82u64; }
    if bytes.equal(word, "atrust_credential_type_declared".as_slice()) { return 83u64; }
    if bytes.equal(word, "atrust_credential_schema_declared".as_slice()) { return 84u64; }
    if bytes.equal(word, "atrust_credential_claims_declared".as_slice()) { return 85u64; }
    if bytes.equal(word, "atrust_credential_verification_declared_only".as_slice()) { return 86u64; }
    if bytes.equal(word, "atrust_credential_presentation_disabled".as_slice()) { return 87u64; }
    if bytes.equal(word, "atrust_credential_resolution_disabled".as_slice()) { return 88u64; }
    if bytes.equal(word, "atrust_credential_key_material_denied".as_slice()) { return 89u64; }
    if bytes.equal(word, "atrust_credential_secret_material_denied".as_slice()) { return 90u64; }
    if bytes.equal(word, "atrust_credential_execution_disabled".as_slice()) { return 91u64; }
    if bytes.equal(word, "atrust_credential_evidence_required".as_slice()) { return 92u64; }
    if bytes.equal(word, "atrust_credential_security_claims_absent".as_slice()) { return 93u64; }
    if bytes.equal(word, "atrust_handshake_declared".as_slice()) { return 94u64; }
    if bytes.equal(word, "atrust_handshake_initiator_responder_valid".as_slice()) { return 95u64; }
    if bytes.equal(word, "atrust_handshake_identities_valid".as_slice()) { return 96u64; }
    if bytes.equal(word, "atrust_handshake_credential_contracts_valid".as_slice()) { return 97u64; }
    if bytes.equal(word, "atrust_handshake_boundary_method_valid".as_slice()) { return 98u64; }
    if bytes.equal(word, "atrust_handshake_mode_dry_run".as_slice()) { return 99u64; }
    if bytes.equal(word, "atrust_handshake_direction_valid".as_slice()) { return 100u64; }
    if bytes.equal(word, "atrust_handshake_challenge_declared_only".as_slice()) { return 101u64; }
    if bytes.equal(word, "atrust_handshake_response_declared_only".as_slice()) { return 102u64; }
    if bytes.equal(word, "atrust_handshake_transcript_evidence_only".as_slice()) { return 103u64; }
    if bytes.equal(word, "atrust_handshake_verification_declared_only".as_slice()) { return 104u64; }
    if bytes.equal(word, "atrust_handshake_resolution_disabled".as_slice()) { return 105u64; }
    if bytes.equal(word, "atrust_handshake_network_denied".as_slice()) { return 106u64; }
    if bytes.equal(word, "atrust_handshake_key_material_denied".as_slice()) { return 107u64; }
    if bytes.equal(word, "atrust_handshake_secret_material_denied".as_slice()) { return 108u64; }
    if bytes.equal(word, "atrust_handshake_execution_disabled".as_slice()) { return 109u64; }
    if bytes.equal(word, "atrust_handshake_evidence_required".as_slice()) { return 110u64; }
    if bytes.equal(word, "atrust_handshake_security_claims_absent".as_slice()) { return 111u64; }
    if bytes.equal(word, "trust_ledgers_declared".as_slice()) { return 112u64; }
    if bytes.equal(word, "trust_ledger_hash_algorithm_declared".as_slice()) { return 113u64; }
    if bytes.equal(word, "trust_ledger_chain_valid".as_slice()) { return 114u64; }
    if bytes.equal(word, "trust_ledger_entries_bound".as_slice()) { return 115u64; }
    if bytes.equal(word, "trust_ledger_append_only".as_slice()) { return 116u64; }
    if bytes.equal(word, "trust_ledger_network_denied".as_slice()) { return 117u64; }
    if bytes.equal(word, "trust_ledger_key_material_denied".as_slice()) { return 118u64; }
    if bytes.equal(word, "trust_ledger_secret_material_denied".as_slice()) { return 119u64; }
    if bytes.equal(word, "trust_ledger_execution_disabled".as_slice()) { return 120u64; }
    if bytes.equal(word, "trust_ledger_evidence_required".as_slice()) { return 121u64; }
    if bytes.equal(word, "trust_ledger_security_claims_absent".as_slice()) { return 122u64; }
    if bytes.equal(word, "trust_ledger_blockchain_absent".as_slice()) { return 123u64; }
    if bytes.equal(word, "trust_ledger_signature_absent".as_slice()) { return 124u64; }
    if bytes.equal(word, "mcp_bridge_contracts_declared".as_slice()) { return 125u64; }
    if bytes.equal(word, "mcp_bridge_agents_bound".as_slice()) { return 126u64; }
    if bytes.equal(word, "mcp_bridge_passports_bound".as_slice()) { return 127u64; }
    if bytes.equal(word, "mcp_bridge_identities_bound".as_slice()) { return 128u64; }
    if bytes.equal(word, "mcp_bridge_boundaries_bound".as_slice()) { return 129u64; }
    if bytes.equal(word, "mcp_bridge_network_denied".as_slice()) { return 130u64; }
    if bytes.equal(word, "mcp_bridge_external_execution_disabled".as_slice()) { return 131u64; }
    if bytes.equal(word, "mcp_bridge_tool_execution_disabled".as_slice()) { return 132u64; }
    if bytes.equal(word, "mcp_bridge_secret_material_denied".as_slice()) { return 133u64; }
    if bytes.equal(word, "mcp_bridge_key_material_denied".as_slice()) { return 134u64; }
    if bytes.equal(word, "mcp_bridge_authentication_non_secret".as_slice()) { return 135u64; }
    if bytes.equal(word, "mcp_bridge_security_claims_absent".as_slice()) { return 136u64; }
    if bytes.equal(word, "a2a_bridge_contracts_declared".as_slice()) { return 137u64; }
    if bytes.equal(word, "a2a_bridge_agents_bound".as_slice()) { return 138u64; }
    if bytes.equal(word, "a2a_bridge_passports_bound".as_slice()) { return 139u64; }
    if bytes.equal(word, "a2a_bridge_identities_bound".as_slice()) { return 140u64; }
    if bytes.equal(word, "a2a_bridge_handshakes_bound".as_slice()) { return 141u64; }
    if bytes.equal(word, "a2a_bridge_trust_ledgers_bound".as_slice()) { return 142u64; }
    if bytes.equal(word, "a2a_bridge_message_contracts_bound".as_slice()) { return 143u64; }
    if bytes.equal(word, "a2a_bridge_network_denied".as_slice()) { return 144u64; }
    if bytes.equal(word, "a2a_bridge_external_execution_disabled".as_slice()) { return 145u64; }
    if bytes.equal(word, "a2a_bridge_agent_execution_disabled".as_slice()) { return 146u64; }
    if bytes.equal(word, "a2a_bridge_secret_material_denied".as_slice()) { return 147u64; }
    if bytes.equal(word, "a2a_bridge_key_material_denied".as_slice()) { return 148u64; }
    if bytes.equal(word, "a2a_bridge_authentication_non_secret".as_slice()) { return 149u64; }
    if bytes.equal(word, "a2a_bridge_security_claims_absent".as_slice()) { return 150u64; }
    if bytes.equal(word, "atrust_evidence_maps_declared".as_slice()) { return 151u64; }
    if bytes.equal(word, "atrust_evidence_map_agents_bound".as_slice()) { return 152u64; }
    if bytes.equal(word, "atrust_evidence_map_passports_bound".as_slice()) { return 153u64; }
    if bytes.equal(word, "atrust_evidence_map_identities_bound".as_slice()) { return 154u64; }
    if bytes.equal(word, "atrust_evidence_map_credentials_bound".as_slice()) { return 155u64; }
    if bytes.equal(word, "atrust_evidence_map_handshakes_bound".as_slice()) { return 156u64; }
    if bytes.equal(word, "atrust_evidence_map_ledgers_bound".as_slice()) { return 157u64; }
    if bytes.equal(word, "atrust_evidence_map_bridges_bound".as_slice()) { return 158u64; }
    if bytes.equal(word, "atrust_evidence_map_policies_bound".as_slice()) { return 159u64; }
    if bytes.equal(word, "atrust_evidence_map_coverage_required".as_slice()) { return 160u64; }
    if bytes.equal(word, "atrust_evidence_map_verification_non_verifying".as_slice()) { return 161u64; }
    if bytes.equal(word, "atrust_evidence_map_resolution_disabled".as_slice()) { return 162u64; }
    if bytes.equal(word, "atrust_evidence_map_network_denied".as_slice()) { return 163u64; }
    if bytes.equal(word, "atrust_evidence_map_external_execution_disabled".as_slice()) { return 164u64; }
    if bytes.equal(word, "atrust_evidence_map_secret_material_denied".as_slice()) { return 165u64; }
    if bytes.equal(word, "atrust_evidence_map_key_material_denied".as_slice()) { return 166u64; }
    if bytes.equal(word, "atrust_evidence_map_execution_disabled".as_slice()) { return 167u64; }
    if bytes.equal(word, "atrust_evidence_map_security_claims_absent".as_slice()) { return 168u64; }
    if bytes.equal(word, "governance_profiles_declared".as_slice()) { return 169u64; }
    if bytes.equal(word, "governance_profiles_evidence_bound".as_slice()) { return 170u64; }
    if bytes.equal(word, "governance_profiles_controls_mapped".as_slice()) { return 171u64; }
    if bytes.equal(word, "governance_profiles_runtime_disabled".as_slice()) { return 172u64; }
    if bytes.equal(word, "governance_profiles_security_claims_absent".as_slice()) { return 173u64; }
    if bytes.equal(word, "governance_profiles_no_legal_certification".as_slice()) { return 174u64; }
    if bytes.equal(word, "regulatory_mappings_declared".as_slice()) { return 175u64; }
    if bytes.equal(word, "regulatory_mappings_profiles_bound".as_slice()) { return 176u64; }
    if bytes.equal(word, "regulatory_mappings_obligations_mapped".as_slice()) { return 177u64; }
    if bytes.equal(word, "regulatory_mappings_controls_bound".as_slice()) { return 178u64; }
    if bytes.equal(word, "regulatory_mappings_legal_claims_absent".as_slice()) { return 179u64; }
    if bytes.equal(word, "regulatory_mappings_certification_absent".as_slice()) { return 180u64; }
    if bytes.equal(word, "regulatory_mappings_runtime_disabled".as_slice()) { return 181u64; }
    if bytes.equal(word, "regulatory_mappings_security_claims_absent".as_slice()) { return 182u64; }
    if bytes.equal(word, "third_party_verifiers_declared".as_slice()) { return 183u64; }
    if bytes.equal(word, "third_party_verifiers_identity_declared".as_slice()) { return 184u64; }
    if bytes.equal(word, "third_party_verifiers_scope_bounded".as_slice()) { return 185u64; }
    if bytes.equal(word, "third_party_verifiers_runtime_disabled".as_slice()) { return 186u64; }
    if bytes.equal(word, "third_party_verifiers_legal_claims_absent".as_slice()) { return 187u64; }
    if bytes.equal(word, "third_party_verifiers_certification_absent".as_slice()) { return 188u64; }
    if bytes.equal(word, "third_party_verifiers_security_claims_absent".as_slice()) { return 189u64; }
    if bytes.equal(word, "public_conformance_reports_declared".as_slice()) { return 190u64; }
    if bytes.equal(word, "public_conformance_reports_verifiers_bound".as_slice()) { return 191u64; }
    if bytes.equal(word, "public_conformance_reports_artifacts_declared".as_slice()) { return 192u64; }
    if bytes.equal(word, "public_conformance_reports_evidence_bound".as_slice()) { return 193u64; }
    if bytes.equal(word, "public_conformance_reports_governance_bound".as_slice()) { return 194u64; }
    if bytes.equal(word, "public_conformance_reports_regulatory_bound".as_slice()) { return 195u64; }
    if bytes.equal(word, "public_conformance_reports_replayable".as_slice()) { return 196u64; }
    if bytes.equal(word, "public_conformance_reports_runtime_disabled".as_slice()) { return 197u64; }
    if bytes.equal(word, "public_conformance_reports_legal_claims_absent".as_slice()) { return 198u64; }
    if bytes.equal(word, "public_conformance_reports_certification_absent".as_slice()) { return 199u64; }
    if bytes.equal(word, "public_conformance_reports_security_claims_absent".as_slice()) { return 200u64; }
    if bytes.equal(word, "runtime_hardening_profiles_declared".as_slice()) { return 201u64; }
    if bytes.equal(word, "runtime_hardening_evidence_bound".as_slice()) { return 202u64; }
    if bytes.equal(word, "runtime_hardening_deny_by_default".as_slice()) { return 203u64; }
    if bytes.equal(word, "runtime_hardening_sandbox_required".as_slice()) { return 204u64; }
    if bytes.equal(word, "runtime_hardening_network_denied".as_slice()) { return 205u64; }
    if bytes.equal(word, "runtime_hardening_external_providers_disabled".as_slice()) { return 206u64; }
    if bytes.equal(word, "runtime_hardening_tool_execution_disabled".as_slice()) { return 207u64; }
    if bytes.equal(word, "runtime_hardening_agent_execution_disabled".as_slice()) { return 208u64; }
    if bytes.equal(word, "runtime_hardening_filesystem_denied".as_slice()) { return 209u64; }
    if bytes.equal(word, "runtime_hardening_env_denied".as_slice()) { return 210u64; }
    if bytes.equal(word, "runtime_hardening_secret_material_denied".as_slice()) { return 211u64; }
    if bytes.equal(word, "runtime_hardening_key_material_denied".as_slice()) { return 212u64; }
    if bytes.equal(word, "runtime_hardening_audit_log_required".as_slice()) { return 213u64; }
    if bytes.equal(word, "runtime_hardening_security_claims_absent".as_slice()) { return 214u64; }
    if bytes.equal(word, "threat_models_declared".as_slice()) { return 215u64; }
    if bytes.equal(word, "threat_models_hardening_bound".as_slice()) { return 216u64; }
    if bytes.equal(word, "threat_models_assets_mapped".as_slice()) { return 217u64; }
    if bytes.equal(word, "threat_models_threats_mapped".as_slice()) { return 218u64; }
    if bytes.equal(word, "threat_models_mitigations_mapped".as_slice()) { return 219u64; }
    if bytes.equal(word, "threat_models_runtime_disabled".as_slice()) { return 220u64; }
    if bytes.equal(word, "threat_models_network_denied".as_slice()) { return 221u64; }
    if bytes.equal(word, "threat_models_secret_material_denied".as_slice()) { return 222u64; }
    if bytes.equal(word, "threat_models_key_material_denied".as_slice()) { return 223u64; }
    if bytes.equal(word, "threat_models_execution_disabled".as_slice()) { return 224u64; }
    if bytes.equal(word, "threat_models_security_claims_absent".as_slice()) { return 225u64; }
    if bytes.equal(word, "spec_freezes_declared".as_slice()) { return 226u64; }
    if bytes.equal(word, "spec_freeze_versions_pinned".as_slice()) { return 227u64; }
    if bytes.equal(word, "spec_freeze_features_declared".as_slice()) { return 228u64; }
    if bytes.equal(word, "spec_freeze_compatibility_declared".as_slice()) { return 229u64; }
    if bytes.equal(word, "spec_freeze_required_suites_declared".as_slice()) { return 230u64; }
    if bytes.equal(word, "spec_freeze_runtime_disabled".as_slice()) { return 231u64; }
    if bytes.equal(word, "spec_freeze_network_denied".as_slice()) { return 232u64; }
    if bytes.equal(word, "spec_freeze_external_execution_disabled".as_slice()) { return 233u64; }
    if bytes.equal(word, "spec_freeze_provider_execution_disabled".as_slice()) { return 234u64; }
    if bytes.equal(word, "spec_freeze_secret_material_denied".as_slice()) { return 235u64; }
    if bytes.equal(word, "spec_freeze_key_material_denied".as_slice()) { return 236u64; }
    if bytes.equal(word, "spec_freeze_env_denied".as_slice()) { return 237u64; }
    if bytes.equal(word, "spec_freeze_filesystem_denied".as_slice()) { return 238u64; }
    if bytes.equal(word, "spec_freeze_security_claims_absent".as_slice()) { return 239u64; }
    if bytes.equal(word, "spec_freeze_legal_claims_absent".as_slice()) { return 240u64; }
    if bytes.equal(word, "spec_freeze_certification_absent".as_slice()) { return 241u64; }
    if bytes.equal(word, "release_candidates_declared".as_slice()) { return 242u64; }
    if bytes.equal(word, "release_candidates_spec_freeze_bound".as_slice()) { return 243u64; }
    if bytes.equal(word, "release_candidates_artifacts_declared".as_slice()) { return 244u64; }
    if bytes.equal(word, "release_candidates_checks_declared".as_slice()) { return 245u64; }
    if bytes.equal(word, "release_candidates_compatibility_matrix_declared".as_slice()) { return 246u64; }
    if bytes.equal(word, "release_candidates_limitations_declared".as_slice()) { return 247u64; }
    if bytes.equal(word, "release_candidates_runtime_disabled".as_slice()) { return 248u64; }
    if bytes.equal(word, "release_candidates_network_denied".as_slice()) { return 249u64; }
    if bytes.equal(word, "release_candidates_external_execution_disabled".as_slice()) { return 250u64; }
    if bytes.equal(word, "release_candidates_provider_execution_disabled".as_slice()) { return 251u64; }
    if bytes.equal(word, "release_candidates_secret_material_denied".as_slice()) { return 252u64; }
    if bytes.equal(word, "release_candidates_key_material_denied".as_slice()) { return 253u64; }
    if bytes.equal(word, "release_candidates_env_denied".as_slice()) { return 254u64; }
    if bytes.equal(word, "release_candidates_filesystem_denied".as_slice()) { return 255u64; }
    if bytes.equal(word, "release_candidates_security_claims_absent".as_slice()) { return 256u64; }
    if bytes.equal(word, "release_candidates_legal_claims_absent".as_slice()) { return 257u64; }
    if bytes.equal(word, "release_candidates_certification_absent".as_slice()) { return 258u64; }
    if bytes.equal(word, "runtime_execution_profiles_declared".as_slice()) { return 259u64; }
    if bytes.equal(word, "runtime_profiles_agents_bound".as_slice()) { return 260u64; }
    if bytes.equal(word, "runtime_profiles_provider_bound".as_slice()) { return 261u64; }
    if bytes.equal(word, "runtime_profiles_hardening_bound".as_slice()) { return 262u64; }
    if bytes.equal(word, "runtime_profiles_evidence_bound".as_slice()) { return 263u64; }
    if bytes.equal(word, "runtime_profiles_governance_bound".as_slice()) { return 264u64; }
    if bytes.equal(word, "runtime_profiles_fail_closed".as_slice()) { return 265u64; }
    if bytes.equal(word, "runtime_profiles_external_execution_sandboxed".as_slice()) { return 266u64; }
    if bytes.equal(word, "runtime_profiles_tool_execution_disabled".as_slice()) { return 267u64; }
    if bytes.equal(word, "runtime_profiles_agent_execution_disabled".as_slice()) { return 268u64; }
    if bytes.equal(word, "runtime_profiles_secret_refs_only".as_slice()) { return 269u64; }
    if bytes.equal(word, "runtime_profiles_security_claims_absent".as_slice()) { return 270u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_declared".as_slice()) { return 271u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_provider_bound".as_slice()) { return 272u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_runtime_bound".as_slice()) { return 273u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_operations_bounded".as_slice()) { return 274u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_network_declared".as_slice()) { return 275u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_external_execution_sandboxed".as_slice()) { return 276u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_tool_execution_disabled".as_slice()) { return 277u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_secret_refs_redacted".as_slice()) { return 278u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_fail_closed".as_slice()) { return 279u64; }
    if bytes.equal(word, "sandboxed_provider_adapters_security_claims_absent".as_slice()) { return 280u64; }
    NO_VARIANT
}

// `parse_policy_violation`: the `PolicyViolationAction` a word names.
pub const T_PARSE_POLICY_VIOLATION__POLICY_VIOLATION_ACTION: u64 = 117u64;
pub fn words_parse_policy_violation__policy_violation_action(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "block".as_slice()) { return 0u64; }
    if bytes.equal(word, "review".as_slice()) { return 1u64; }
    if bytes.equal(word, "warn".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_capability`: the `CapabilityLevel` a word names.
pub const T_PARSE_CAPABILITY__CAPABILITY_LEVEL: u64 = 118u64;
pub fn words_parse_capability__capability_level(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "safe".as_slice()) { return 0u64; }
    if bytes.equal(word, "restricted".as_slice()) { return 1u64; }
    if bytes.equal(word, "dangerous".as_slice()) { return 2u64; }
    NO_VARIANT
}

// `parse_type`: the `MessageFieldType` a word names.
pub const T_PARSE_TYPE__MESSAGE_FIELD_TYPE: u64 = 119u64;
pub fn words_parse_type__message_field_type(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "string".as_slice()) { return 0u64; }
    if bytes.equal(word, "bool".as_slice()) { return 1u64; }
    if bytes.equal(word, "int".as_slice()) { return 2u64; }
    if bytes.equal(word, "float".as_slice()) { return 3u64; }
    NO_VARIANT
}

// `parse_agent`: the `Approval` a word names.
pub const T_PARSE_AGENT__APPROVAL: u64 = 120u64;
pub fn words_parse_agent__approval(word: Slice<u8>) -> u64 {
    if bytes.equal(word, "granted".as_slice()) { return 0u64; }
    if bytes.equal(word, "denied".as_slice()) { return 1u64; }
    NO_VARIANT
}

// The variant `word` names in table `table` (a `T_` constant), or
// NO_VARIANT.
pub fn lookup(table: u64, word: Slice<u8>) -> u64 {
    if table == 0u64 { return words_parse_bridge_direction__bridge_direction(word); }
    if table == 1u64 { return words_parse_bridge_execution__a_trust_execution(word); }
    if table == 2u64 { return words_parse_bridge_material__a_trust_material_boundary(word); }
    if table == 3u64 { return words_parse_bridge_authentication__bridge_authentication(word); }
    if table == 4u64 { return words_parse_bridge_authorization__bridge_authorization(word); }
    if table == 5u64 { return words_parse_governance_scope__governance_scope(word); }
    if table == 6u64 { return words_parse_governance_level__governance_level(word); }
    if table == 7u64 { return words_parse_governance_domain__governance_domain(word); }
    if table == 8u64 { return words_parse_governance_risk_level__governance_risk_level(word); }
    if table == 9u64 { return words_parse_governance_review_status__governance_review_status(word); }
    if table == 10u64 { return words_parse_governance_assurance__governance_assurance(word); }
    if table == 11u64 { return words_parse_governance_control_category__governance_control_category(word); }
    if table == 12u64 { return words_parse_governance_control_status__governance_control_status(word); }
    if table == 13u64 { return words_parse_regulatory_coverage__regulatory_coverage(word); }
    if table == 14u64 { return words_parse_regulatory_assessment__regulatory_assessment(word); }
    if table == 15u64 { return words_parse_regulatory_obligation_status__regulatory_obligation_status(word); }
    if table == 16u64 { return words_parse_third_party_verifier_type__third_party_verifier_type(word); }
    if table == 17u64 { return words_parse_verifier_independence__verifier_independence(word); }
    if table == 18u64 { return words_parse_verifier_identity_mode__verifier_identity_mode(word); }
    if table == 19u64 { return words_parse_verifier_verification_mode__verifier_verification_mode(word); }
    if table == 20u64 { return words_parse_public_conformance_result__public_conformance_result(word); }
    if table == 21u64 { return words_parse_public_conformance_reproducibility__public_conformance_reproducibility(word); }
    if table == 22u64 { return words_parse_public_conformance_review_status__public_conformance_review_status(word); }
    if table == 23u64 { return words_parse_public_conformance_claim_category__public_conformance_claim_category(word); }
    if table == 24u64 { return words_parse_public_conformance_claim_status__public_conformance_claim_status(word); }
    if table == 25u64 { return words_parse_governance_network__a_trust_network_boundary(word); }
    if table == 26u64 { return words_parse_governance_security_claims__a_trust_security_claims(word); }
    if table == 27u64 { return words_parse_provider__provider_kind_decl(word); }
    if table == 28u64 { return words_parse_harness__harness_mode(word); }
    if table == 29u64 { return words_parse_harness__harness_network(word); }
    if table == 30u64 { return words_parse_harness__harness_secrets(word); }
    if table == 31u64 { return words_parse_harness__harness_filesystem(word); }
    if table == 32u64 { return words_parse_feature__feature_status(word); }
    if table == 33u64 { return words_parse_feature__feature_default(word); }
    if table == 34u64 { return words_parse_secret__secret_scope(word); }
    if table == 35u64 { return words_parse_secret__secret_access(word); }
    if table == 36u64 { return words_parse_secret__secret_source(word); }
    if table == 37u64 { return words_parse_adapter__adapter_kind(word); }
    if table == 38u64 { return words_parse_adapter__adapter_mode(word); }
    if table == 39u64 { return words_parse_adapter__adapter_execution(word); }
    if table == 40u64 { return words_parse_adapter__adapter_network(word); }
    if table == 41u64 { return words_parse_adapter__adapter_secrets(word); }
    if table == 42u64 { return words_parse_adapter__adapter_filesystem(word); }
    if table == 43u64 { return words_parse_adapter_profile__adapter_profile_family(word); }
    if table == 44u64 { return words_parse_adapter_profile__adapter_profile_api_style(word); }
    if table == 45u64 { return words_parse_adapter_profile__adapter_profile_auth(word); }
    if table == 46u64 { return words_parse_adapter_profile__adapter_profile_execution(word); }
    if table == 47u64 { return words_parse_adapter_profile__adapter_profile_network(word); }
    if table == 48u64 { return words_parse_adapter_profile__adapter_profile_secrets(word); }
    if table == 49u64 { return words_parse_crypto__crypto_kind(word); }
    if table == 50u64 { return words_parse_crypto__crypto_status(word); }
    if table == 51u64 { return words_parse_crypto__crypto_strength(word); }
    if table == 52u64 { return words_parse_did_method__did_method_status(word); }
    if table == 53u64 { return words_parse_did_method__did_resolution_mode(word); }
    if table == 54u64 { return words_parse_did_method__did_ledger_mode(word); }
    if table == 55u64 { return words_parse_atrust_boundary__a_trust_identity_format(word); }
    if table == 56u64 { return words_parse_atrust_boundary__a_trust_credential_mode(word); }
    if table == 57u64 { return words_parse_atrust_boundary__a_trust_handshake_mode(word); }
    if table == 58u64 { return words_parse_atrust_boundary__a_trust_resolution_mode(word); }
    if table == 59u64 { return words_parse_atrust_boundary__a_trust_material_boundary(word); }
    if table == 60u64 { return words_parse_atrust_boundary__a_trust_execution(word); }
    if table == 61u64 { return words_parse_atrust_boundary__a_trust_post_quantum_requirement(word); }
    if table == 62u64 { return words_parse_atrust_boundary__a_trust_security_claims(word); }
    if table == 63u64 { return words_parse_atrust_identity__a_trust_identity_status(word); }
    if table == 64u64 { return words_parse_atrust_identity__a_trust_identity_validation(word); }
    if table == 65u64 { return words_parse_atrust_identity__a_trust_resolution_mode(word); }
    if table == 66u64 { return words_parse_atrust_identity__a_trust_material_boundary(word); }
    if table == 67u64 { return words_parse_atrust_identity__a_trust_execution(word); }
    if table == 68u64 { return words_parse_atrust_identity__a_trust_evidence_requirement(word); }
    if table == 69u64 { return words_parse_atrust_identity__a_trust_security_claims(word); }
    if table == 70u64 { return words_parse_atrust_credential_contract__a_trust_credential_status(word); }
    if table == 71u64 { return words_parse_atrust_credential_contract__a_trust_credential_verification(word); }
    if table == 72u64 { return words_parse_atrust_credential_contract__a_trust_credential_presentation(word); }
    if table == 73u64 { return words_parse_atrust_credential_contract__a_trust_resolution_mode(word); }
    if table == 74u64 { return words_parse_atrust_credential_contract__a_trust_material_boundary(word); }
    if table == 75u64 { return words_parse_atrust_credential_contract__a_trust_execution(word); }
    if table == 76u64 { return words_parse_atrust_credential_contract__a_trust_evidence_requirement(word); }
    if table == 77u64 { return words_parse_atrust_credential_contract__a_trust_security_claims(word); }
    if table == 78u64 { return words_parse_atrust_handshake__a_trust_handshake_dry_run_mode(word); }
    if table == 79u64 { return words_parse_atrust_handshake__a_trust_handshake_direction(word); }
    if table == 80u64 { return words_parse_atrust_handshake__a_trust_handshake_challenge(word); }
    if table == 81u64 { return words_parse_atrust_handshake__a_trust_handshake_response(word); }
    if table == 82u64 { return words_parse_atrust_handshake__a_trust_handshake_transcript(word); }
    if table == 83u64 { return words_parse_atrust_handshake__a_trust_handshake_verification(word); }
    if table == 84u64 { return words_parse_atrust_handshake__a_trust_resolution_mode(word); }
    if table == 85u64 { return words_parse_atrust_handshake__a_trust_network_boundary(word); }
    if table == 86u64 { return words_parse_atrust_handshake__a_trust_material_boundary(word); }
    if table == 87u64 { return words_parse_atrust_handshake__a_trust_execution(word); }
    if table == 88u64 { return words_parse_atrust_handshake__a_trust_evidence_requirement(word); }
    if table == 89u64 { return words_parse_atrust_handshake__a_trust_security_claims(word); }
    if table == 90u64 { return words_parse_trust_ledger__trust_ledger_scope(word); }
    if table == 91u64 { return words_parse_trust_ledger__trust_ledger_mode(word); }
    if table == 92u64 { return words_parse_trust_ledger__trust_ledger_chain_policy(word); }
    if table == 93u64 { return words_parse_trust_ledger__a_trust_network_boundary(word); }
    if table == 94u64 { return words_parse_trust_ledger__a_trust_material_boundary(word); }
    if table == 95u64 { return words_parse_trust_ledger__a_trust_execution(word); }
    if table == 96u64 { return words_parse_trust_ledger__a_trust_evidence_requirement(word); }
    if table == 97u64 { return words_parse_trust_ledger__a_trust_security_claims(word); }
    if table == 98u64 { return words_parse_trust_ledger_entry__trust_ledger_entry_kind(word); }
    if table == 99u64 { return words_parse_mcp_bridge_contract__bridge_transport(word); }
    if table == 100u64 { return words_parse_mcp_bridge_contract__mcp_protocol(word); }
    if table == 101u64 { return words_parse_mcp_bridge_contract__a_trust_network_boundary(word); }
    if table == 102u64 { return words_parse_mcp_bridge_contract__a_trust_evidence_requirement(word); }
    if table == 103u64 { return words_parse_mcp_bridge_contract__a_trust_security_claims(word); }
    if table == 104u64 { return words_parse_a2a_bridge_contract__a2_a_protocol(word); }
    if table == 105u64 { return words_parse_a2a_bridge_contract__bridge_transport(word); }
    if table == 106u64 { return words_parse_a2a_bridge_contract__a_trust_network_boundary(word); }
    if table == 107u64 { return words_parse_a2a_bridge_contract__a_trust_evidence_requirement(word); }
    if table == 108u64 { return words_parse_a2a_bridge_contract__a_trust_security_claims(word); }
    if table == 109u64 { return words_parse_atrust_evidence_map__a_trust_evidence_map_coverage(word); }
    if table == 110u64 { return words_parse_atrust_evidence_map__a_trust_evidence_map_mapping_mode(word); }
    if table == 111u64 { return words_parse_atrust_evidence_map__a_trust_handshake_verification(word); }
    if table == 112u64 { return words_parse_atrust_evidence_map__a_trust_resolution_mode(word); }
    if table == 113u64 { return words_parse_atrust_evidence_map__a_trust_evidence_requirement(word); }
    if table == 114u64 { return words_parse_atrust_evidence_map__a_trust_network_boundary(word); }
    if table == 115u64 { return words_parse_atrust_evidence_map__a_trust_security_claims(word); }
    if table == 116u64 { return words_parse_policy_rule__policy_rule(word); }
    if table == 117u64 { return words_parse_policy_violation__policy_violation_action(word); }
    if table == 118u64 { return words_parse_capability__capability_level(word); }
    if table == 119u64 { return words_parse_type__message_field_type(word); }
    if table == 120u64 { return words_parse_agent__approval(word); }
    NO_VARIANT
}
